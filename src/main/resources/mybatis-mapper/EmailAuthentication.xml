<?xml version="1.0" encoding="UTF-8"?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.or.kosa.nux2.domain.member.mapper.EmailAuthenticationMapper">
    <insert id="insertOrUpdateAuthenticationInfo" parameterType="map">
        MERGE INTO email_authentication
        USING dual
        ON (member_id = #{memberId})
        WHEN MATCHED THEN
        UPDATE SET
        authentication_number = #{authenticationNumber},
        create_time = SYSTIMESTAMP
        WHEN NOT MATCHED THEN
        insert
            (member_id, authentication_number)
        values
            (#{memberId},#{authenticationNumber})
    </insert>
    <select id="findAuthenticationNumberByMemberIdAndTimeDiffLessThanFiveMinute" parameterType="kr.or.kosa.nux2.domain.member.dto.MemberDto$MemberIdRequest" resultType="kr.or.kosa.nux2.domain.member.dto.MemberDto$AuthenticationDto">
        SELECT authentication_number authenticationNumber
        FROM email_authentication
        WHERE create_time >= (SYSTIMESTAMP - INTERVAL '5' MINUTE) AND member_id = #{memberId}

    </select>
    <delete id="deleteAuthenticationInfoByMemberId" parameterType="kr.or.kosa.nux2.domain.member.dto.MemberDto$MemberIdRequest">
        delete from email_authentication
        where member_id = #{memberId}
    </delete>
</mapper>